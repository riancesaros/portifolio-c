#include <studio.h>
#include <math.h>

int main () {
    int number1; number2, type;
    
    printf("Olá, bem vindo a calculadora de Potenciação e Raiz quadrada.");
    printf("\nPara usar a função de potencia digite 'p' \nou digite 'r' para usar raiz quadrada.");
    scanf ("%i", &type);
    
    if (type == 'p' || 'P') {
             
             
    }
    else if (type == 'r' || 'R') {
             
             
    }
    else {
         
    }
}
